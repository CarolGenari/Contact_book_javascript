// Projeto Agenda: Criando o Template HTML(EJS)

// Para CSS, vamos usar um pacote do Bootstrap e vamos usar o template que está
// em: Starter template
// Vamos copiar o código de HTML e vamos colar no nosso arquivo "index.ejs"
// Depois vamos dividir as partes desse código html em arquivos parciais, que
// ficarão na pasta "includes"
// Não podemos esquecer de carregar nossos scripts, que estão no arquivo "bundle.js"
// <script src="./assets/js/bundle.js"></script>

// Vamos sempre deixar o servidor rodando (npm start) e o webpack(npm run dev)

// No site do Bootstrap, vamos copiar o código para uma navbar e criar um arquivo
// parcial, dentro da pasta "includes" com esse código, como fizemos para o resto

// Fizemos o código para o erro 404, que está no arquivo "404.ejs"
